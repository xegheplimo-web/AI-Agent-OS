{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the AI Agent OS shell",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
